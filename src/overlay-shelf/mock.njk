---
layout: landing.njk
title: Overlay Shelf mock | Ionrift Workshop
description: UX mock for Overlay Shelf redesign. Not the live download page.
canonical: https://ionrift.cloud/overlay-shelf/mock/
ogTitle: Overlay Shelf mock
ogDescription: Review mock for a shorter Overlay Shelf with install guidance and overlays-only zip.
bodyClass: page-overlay-shelf page-overlay-shelf-mock
noindex: true
eleventyExcludeFromCollections: true
---

{% include "landing/nav.njk" %}

<main class="page-main page-overlay-shelf page-overlay-shelf-mock">
    <div class="container">
        <p class="shelf-mock-banner" role="status">
            <strong>Mock.</strong> Layout and copy for review. Downloads and zip compile are not wired here.
            Live page remains <a href="/overlay-shelf/">/overlay-shelf/</a>.
        </p>

        <nav class="page-breadcrumb" aria-label="Breadcrumb">
            <a href="/">Home</a>
            <span aria-hidden="true">/</span>
            <a href="/overlay-shelf/">Overlay Shelf</a>
            <span aria-hidden="true">/</span>
            <span>Mock</span>
        </nav>

        {# ── Hero: one job ── #}
        <header class="shelf-hero shelf-hero--mock">
            <div class="section-label">Downloads</div>
            <h1 class="shelf-title">Overlay Shelf</h1>
            <p class="shelf-lead">
                Browser download, then unzip into Foundry’s Data folder. Patreon links here.
                Nothing on this page installs from inside Foundry.
            </p>
            <div class="shelf-hero-actions">
                <button type="button" class="btn btn-primary" disabled title="Mock only">Download selected overlays</button>
                <a class="btn btn-secondary" href="#install">Where files go</a>
            </div>
            <label class="shelf-check shelf-check--inline">
                <input type="checkbox" checked disabled>
                <span>Include optional cooking item icons</span>
            </label>
        </header>

        {# ── Overlay groups (cards, not spreadsheet) ── #}
        <section class="shelf-section shelf-section--tight" aria-labelledby="shelf-packs-heading">
            <div class="shelf-section-head">
                <h2 id="shelf-packs-heading" class="shelf-h2">Overlays</h2>
                <p class="shelf-note">Select Free packs to include in one overlays-only zip, or download a single pack.</p>
            </div>

            <div class="shelf-module-block">
                <h3 class="shelf-module-label">Respite</h3>
                <div class="shelf-tile-grid">
                    <article class="shelf-tile shelf-tile--selected">
                        <label class="shelf-tile-select"><input type="checkbox" checked disabled> <span class="visually-hidden">Select Core pack</span></label>
                        <h4 class="shelf-tile-title">Core pack</h4>
                        <p class="shelf-tile-meta">Free · v0.2.3</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                    <article class="shelf-tile shelf-tile--selected">
                        <label class="shelf-tile-select"><input type="checkbox" checked disabled> <span class="visually-hidden">Select Cooking</span></label>
                        <h4 class="shelf-tile-title">Core Cooking Pack</h4>
                        <p class="shelf-tile-meta">Free · v1.0.10</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                    <article class="shelf-tile">
                        <label class="shelf-tile-select"><input type="checkbox" checked disabled> <span class="visually-hidden">Select icons</span></label>
                        <h4 class="shelf-tile-title">Cooking item icons</h4>
                        <p class="shelf-tile-meta">Free · optional</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                    <article class="shelf-tile">
                        <label class="shelf-tile-select"><input type="checkbox" disabled> <span class="visually-hidden">Select Wanderers</span></label>
                        <h4 class="shelf-tile-title">Wanderer’s pack</h4>
                        <p class="shelf-tile-meta">Free · v1.0.0</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                    <article class="shelf-tile shelf-tile--gated">
                        <h4 class="shelf-tile-title">Craft Professions</h4>
                        <p class="shelf-tile-meta">Initiate · on Patreon</p>
                        <a class="btn btn-secondary btn-sm" href="{{ site.patreonPackCollectionUrl }}" target="_blank" rel="noopener">Patreon</a>
                    </article>
                </div>
            </div>

            <div class="shelf-module-block">
                <h3 class="shelf-module-label">Quartermaster</h3>
                <div class="shelf-tile-grid">
                    <article class="shelf-tile shelf-tile--selected">
                        <label class="shelf-tile-select"><input type="checkbox" checked disabled> <span class="visually-hidden">Select QM Core</span></label>
                        <h4 class="shelf-tile-title">Core pack</h4>
                        <p class="shelf-tile-meta">Free · v0.3.1</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                    <article class="shelf-tile">
                        <label class="shelf-tile-select"><input type="checkbox" disabled> <span class="visually-hidden">Select Bone Dust</span></label>
                        <h4 class="shelf-tile-title">Bone &amp; Dust</h4>
                        <p class="shelf-tile-meta">Free · v0.1.4</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                </div>
            </div>

            <div class="shelf-module-block">
                <h3 class="shelf-module-label">Resonance</h3>
                <div class="shelf-tile-grid">
                    <article class="shelf-tile">
                        <label class="shelf-tile-select"><input type="checkbox" disabled> <span class="visually-hidden">Select Resonance core</span></label>
                        <h4 class="shelf-tile-title">Core SFX</h4>
                        <p class="shelf-tile-meta">Free · v0.1.0</p>
                        <button type="button" class="btn btn-secondary btn-sm" disabled>Download</button>
                    </article>
                </div>
            </div>
        </section>

        {# ── Install teaching ── #}
        <section class="shelf-section" id="install" aria-labelledby="install-heading">
            <h2 id="install-heading" class="shelf-h2">Where <code>ionrift-data</code> lives</h2>
            <p class="shelf-note">
                Foundry’s <strong>Data</strong> folder is the root. Create <code>ionrift-data</code> next to <code>modules</code> and <code>worlds</code> if it is not there yet.
                Unzip so each pack’s <code>overlay-manifest.json</code> sits in its subfolder, then reload the world.
            </p>

            <div class="shelf-fs-grid">
                <figure class="shelf-fs-panel">
                    <figcaption>Before</figcaption>
                    <pre class="shelf-fs-tree" aria-label="Data folder before overlays">Data/
├── modules/
├── systems/
├── worlds/
└── (no ionrift-data yet)</pre>
                </figure>
                <figure class="shelf-fs-panel shelf-fs-panel--after">
                    <figcaption>After (example)</figcaption>
                    <pre class="shelf-fs-tree" aria-label="Data folder after overlays">Data/
├── modules/
├── systems/
├── worlds/
└── ionrift-data/
    └── overlays/
        ├── ionrift-respite/
        │   ├── core/
        │   │   └── overlay-manifest.json
        │   └── cooking/
        │       └── overlay-manifest.json
        └── ionrift-quartermaster/
            └── core/
                └── overlay-manifest.json</pre>
                </figure>
            </div>

            <ul class="shelf-host-notes">
                <li><strong>Self-hosted:</strong> open the Foundry User Data path from Setup, or your host’s file browser for that volume.</li>
                <li><strong>Hosted Foundry:</strong> use the host’s asset / file manager and place the same folder tree under Data.</li>
                <li><strong>Compiled zip:</strong> overlays only. Extract at the Data root so <code>ionrift-data/overlays/...</code> lands in the right place. Module zips are separate.</li>
            </ul>
        </section>

        {# ── Confirm with macro ── #}
        <details class="shelf-confirm">
            <summary>Confirm with the status macro</summary>
            <p class="shelf-note">
                Unsure whether the unzip worked? Paste this Script macro into Foundry, run it, open F12.
                It compares disk to the current listing (latest / outdated / missing). It is not part of any Ionrift module.
            </p>
            <p class="shelf-note">Full macro text lives on the live <a href="/overlay-shelf/">Overlay Shelf</a> page (copy button).</p>
        </details>

        {# ── Modules secondary ── #}
        <section class="shelf-modules-strip" aria-labelledby="modules-strip-heading">
            <h2 id="modules-strip-heading" class="shelf-h2">Foundry modules</h2>
            <p class="shelf-note">
                Cursewright, Monstrous Feast, and Cartographer are module zips. Install through Foundry Add-on Modules.
                They are not part of the overlays zip.
            </p>
            <div class="shelf-module-links">
                <a class="btn btn-secondary btn-sm" href="{{ site.patreonUrl }}" target="_blank" rel="noopener">Patreon modules</a>
            </div>
        </section>

        <p class="page-back">
            <a href="/overlay-shelf/">&larr; Back to live Overlay Shelf</a>
        </p>
    </div>
</main>

{% include "landing/footer.njk" %}
